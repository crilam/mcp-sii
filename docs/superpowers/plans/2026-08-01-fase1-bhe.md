# Fase 1 — Boletas de honorarios electrónicas: plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Exponer las boletas de honorarios electrónicas del RUT persona autenticado como tools MCP, consultadas por HTTP directo contra los CGI del SII.

**Architecture:** Se agrega un transporte HTTP (`SiiHttpClient`) que reusa la sesión que ya administra `SessionManager`, en vez de manejar el navegador. Los CGI de BHE devuelven HTML cuyos datos viven en variables JavaScript `xml_values`, así que el parser trabaja sobre el fuente. Un scraper por dominio (`bhe.ts`) y su registro de tools (`tools/bhe.ts`), siguiendo el patrón que ya usan mipyme y bienes raíces.

**Tech Stack:** TypeScript, `@modelcontextprotocol/sdk`, `zod`, Jest. Sin dependencias nuevas: el transporte usa `curl` vía `execSync`, igual que la autenticación por certificado que ya existe.

**Spec:** `docs/superpowers/specs/2026-08-01-mcp-sii-persona-natural-design.md`

## Global Constraints

- **Una sola sesión por proceso.** `SessionManager` es el único dueño del cookie jar. `SiiHttpClient` lo obtiene de ahí y nunca autentica por su cuenta. Abrir una segunda sesión dispara el bloqueo del SII `01.01.190.500.720.27`.
- **Encoding ISO-8859-1.** Todas las respuestas del SII, incluidas las JSON. Decodificar como UTF-8 corrompe cualquier texto con tilde. Al leer archivos del SII con herramientas de línea de comandos, `grep` los trata como binarios y falla en silencio: usar `grep -a`.
- **Ninguna tool de persona natural exige `SII_EMPRESA_RUT`.** Usan `authenticateOnly()`, no `getSession()`.
- **Fixtures anonimizadas.** Las respuestas del SII traen RUT, nombre, montos y receptores reales. Toda fixture se anonimiza **preservando estructura y largo de campos** antes de versionarse: el parsing depende del formato, y un reemplazo descuidado hace que los tests validen algo que no existe.
- **Base de los CGI de BHE:** `https://loa.sii.cl/cgi_IMT/`
- **Resultado vacío legítimo ≠ error.** Un `200` parseable con cero filas es una respuesta válida. Un cuerpo que no matchea la estructura esperada es un error.

## Contratos resueltos por la spike de Fase 0

Estos datos están verificados contra el portal real. No hay que redescubrirlos.

| Informe | CGI | Método | Parámetros |
|---|---|---|---|
| Anual emitidas | `TMBCOC_InformeAnualBhe.cgi` | GET | `rut_arrastre`, `dv_arrastre`, `cbanoinformeanual` |
| Mensual emitidas | `TMBCOC_InformeMensualBhe.cgi` | POST | `rut_arrastre`, `dv_arrastre`, `cbmesinformemensual`, `cbanoinformemensual` |
| Diario emitidas | `TMBCOC_InformeDiarioBhe.cgi` | POST | `rut_arrastre`, `dv_arrastre`, `cbdiainformediario`, `cbmesinformediario`, `cbanoinformediario` |
| Recibidas | los mismos con sufijo `Rec` antes de `.cgi` | | |

**Los datos no están en el HTML.** El CGI devuelve la tabla vacía y escribe los valores en variables JavaScript con la forma `xml_values['<mes><columna>'] = "<valor>";`, donde `<mes>` es `ene`…`dic` y `<columna>` es un dígito:

| Sufijo | Campo |
|---|---|
| 1 | honorario bruto |
| 2 | retención de terceros |
| 3 | retención del contribuyente |
| 4 | folio inicial del mes |
| 5 | folio final del mes |
| 6 | emisiones vigentes |
| 7 | emisiones anuladas |

Los totales de folio vienen como `tot4` y `tot5`. La cabecera trae además `nombre_contribuyente`, `rut_arrastre`, `dv_arrastre` y `anio_consulta`. Un mes sin actividad deja las claves ausentes o vacías.

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `src/http.ts` (crear) | `SiiHttpClient`: GET/POST contra el SII con la sesión compartida y decodificación ISO-8859-1. Sin conocimiento de ningún dominio. |
| `src/session.ts` (modificar) | Exponer el cookie jar. Único dueño de la sesión. |
| `src/scrapers/bhe.ts` (crear) | Tipos y parsing de los informes de BHE. Sin I/O propio: recibe el cliente. |
| `src/tools/bhe.ts` (crear) | Registro de las tools BHE en el `McpServer`. |
| `src/server.ts` (modificar) | Cablear cliente, scraper y tools. |
| `tests/fixtures/bhe-informe-anual.html` (crear) | Respuesta real anonimizada del CGI anual. |
| `tests/fixtures/bhe-informe-anual-vacio.html` (crear) | Año sin boletas. |
| `tests/http.test.ts`, `tests/scrapers/bhe.test.ts`, `tests/tools/bhe.test.ts` (crear) | |

## Desviación respecto del spec

El spec propone extraer la autenticación por certificado a `src/auth/certificado.ts`, para que el cliente HTTP y el navegador la compartan. **Este plan no lo hace.**

El objetivo de esa extracción era garantizar una sola sesión por proceso. La Tarea 1 lo consigue de forma más directa: `SessionManager` sigue siendo el único que autentica y simplemente expone su cookie jar. Mover el código de lugar no agrega nada mientras haya un solo dueño de la sesión, y sí agrega un archivo más que mantener.

Si más adelante algo necesita autenticar sin pasar por `SessionManager`, la extracción vuelve a tener sentido. Hoy no.

## Alcance de este plan

Cubre las tools de **consulta**: `sii_bhe_resumen`, `sii_bhe_list_emitidas` y `sii_bhe_list_recibidas`.

`sii_bhe_emitir` y `sii_bhe_anular` **no** entran acá. La spike mapeó la cadena de cinco pasos de la emisión y confirmó que el dry-run es implementable deteniéndose en `TMBECN_ConfirmaTimbrajeContrib.cgi`, pero no se ejecutó ningún paso con datos de un receptor real, así que los nombres exactos de los campos del formulario no están verificados. Escribir ese código ahora sería inventarlo. La Tarea 7 es la spike que los resuelve; la emisión se planifica después, con contratos reales.

---

### Task 1: Exponer el cookie jar en `SessionManager`

`SiiHttpClient` necesita las cookies de la sesión. La restricción de diseño es que no las obtenga autenticando por su cuenta: el proceso debe tener una sola sesión.

**Files:**
- Modify: `src/session.ts`
- Test: `tests/session.cookiejar.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces: `SessionManager.rutaCookieJar(): Promise<string>` — autentica si hace falta y devuelve la ruta del archivo de cookies en formato Netscape que `curl` puede consumir con `-b`.

- [ ] **Step 1: Write the failing test**

```typescript
// tests/session.cookiejar.test.ts
import * as os from 'os';
import * as path from 'path';
import { execSync } from 'child_process';
import { SessionManager } from '../src/session';
import { Browser } from '../src/browser';
import { AuthStrategy, SiiConfig } from '../src/env';

jest.mock('../src/browser');
jest.mock('child_process');
jest.mock('fs');

const MockBrowser = Browser as jest.MockedClass<typeof Browser>;
const mockExec = execSync as jest.MockedFunction<typeof execSync>;

const configCert: SiiConfig = {
  rut: '12345678',
  strategy: AuthStrategy.Certificate,
  certPath: '/ruta/cert.pfx',
  certPassword: 'clave-pfx',
};

const RESPUESTA_OK = "<script>location.replace('https://mipyme.sii.cl/')</script>";

beforeEach(() => {
  jest.clearAllMocks();
  mockExec.mockImplementation(((cmd: string) => {
    if (/version/.test(cmd)) return 'OpenSSL 3.2.0 \n';
    if (/pkcs12/.test(cmd)) return '';
    if (/curl/.test(cmd)) return RESPUESTA_OK;
    return '';
  }) as unknown as typeof execSync);
});

describe('SessionManager.rutaCookieJar', () => {
  it('devuelve la ruta del cookie jar de la sesión', async () => {
    const mgr = new SessionManager(configCert, new MockBrowser());

    const ruta = await mgr.rutaCookieJar();

    expect(ruta).toBe(path.join(os.tmpdir(), 'sii_cookies.txt'));
  });

  // Si el cliente HTTP autenticara por su cuenta, el proceso abriría dos
  // sesiones y el SII bloquearía el RUT (01.01.190.500.720.27).
  it('reusa la sesión ya abierta en vez de autenticar de nuevo', async () => {
    const mgr = new SessionManager(configCert, new MockBrowser());

    await mgr.authenticateOnly();
    await mgr.rutaCookieJar();
    await mgr.rutaCookieJar();

    const autenticaciones = mockExec.mock.calls.filter(([cmd]) =>
      /curl/.test(String(cmd))
    );
    expect(autenticaciones).toHaveLength(1);
  });

  it('autentica si todavía no hay sesión', async () => {
    const mgr = new SessionManager(configCert, new MockBrowser());

    await mgr.rutaCookieJar();

    const autenticaciones = mockExec.mock.calls.filter(([cmd]) =>
      /curl/.test(String(cmd))
    );
    expect(autenticaciones).toHaveLength(1);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx jest tests/session.cookiejar.test.ts`
Expected: FAIL — `mgr.rutaCookieJar is not a function`

- [ ] **Step 3: Write minimal implementation**

En `src/session.ts`, promover la ruta del cookie jar a constante de módulo y agregar el método público. La ruta ya se calcula dentro de `loginWithCert()` como `path.join(tmpDir, 'sii_cookies.txt')`; se extrae para que ambos la compartan.

```typescript
// Junto a las demás constantes de módulo:
const COOKIE_JAR = path.join(os.tmpdir(), 'sii_cookies.txt');

// Dentro de SessionManager, como método público:

  // El cliente HTTP necesita las cookies, pero no debe autenticar por su
  // cuenta: dos sesiones simultáneas contra el mismo RUT disparan el bloqueo
  // del SII. La sesión la administra sólo esta clase.
  async rutaCookieJar(): Promise<string> {
    await this.authenticate();
    return COOKIE_JAR;
  }
```

Dentro de `loginWithCert()`, reemplazar la declaración local por la constante:

```typescript
    const tmpDir = os.tmpdir();
    const certPem = path.join(tmpDir, 'sii_cert.pem');
    const keyPem = path.join(tmpDir, 'sii_key.pem');
    const cookiesFile = COOKIE_JAR;
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx jest tests/session.cookiejar.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 5: Run the whole suite for regressions**

Run: `npx jest && npx tsc --noEmit`
Expected: todo verde. `session.cert.test.ts` sigue pasando: la ruta no cambió de valor, sólo de lugar.

- [ ] **Step 6: Commit**

```bash
git add src/session.ts tests/session.cookiejar.test.ts
git commit -m "feat: exponer el cookie jar de la sesión para el cliente HTTP"
```

---

### Task 2: `SiiHttpClient`

**Files:**
- Create: `src/http.ts`
- Test: `tests/http.test.ts`

**Interfaces:**
- Consumes: `SessionManager.rutaCookieJar(): Promise<string>` (Tarea 1).
- Produces:
  - `class SiiHttpClient { constructor(session: SessionManager) }`
  - `get(url: string, params?: Record<string, string>): Promise<string>`
  - `postForm(url: string, campos: Record<string, string>): Promise<string>`

- [ ] **Step 1: Write the failing test**

```typescript
// tests/http.test.ts
import { execSync } from 'child_process';
import { SiiHttpClient } from '../src/http';
import { SessionManager } from '../src/session';

jest.mock('child_process');
jest.mock('../src/session');

const mockExec = execSync as jest.MockedFunction<typeof execSync>;
const MockSession = SessionManager as jest.MockedClass<typeof SessionManager>;

function makeClient() {
  const session = new MockSession({} as any, {} as any);
  (session.rutaCookieJar as jest.Mock).mockResolvedValue('/tmp/sii_cookies.txt');
  return { session, client: new SiiHttpClient(session) };
}

beforeEach(() => jest.clearAllMocks());

describe('SiiHttpClient.get', () => {
  it('manda las cookies de la sesión compartida', async () => {
    mockExec.mockReturnValue('<html>ok</html>' as never);
    const { client } = makeClient();

    await client.get('https://loa.sii.cl/cgi_IMT/X.cgi');

    const cmd = String(mockExec.mock.calls[0][0]);
    expect(cmd).toContain('-b "/tmp/sii_cookies.txt"');
  });

  it('agrega los parámetros al query string', async () => {
    mockExec.mockReturnValue('<html>ok</html>' as never);
    const { client } = makeClient();

    await client.get('https://loa.sii.cl/cgi_IMT/X.cgi', { anio: '2025', dv: '4' });

    const cmd = String(mockExec.mock.calls[0][0]);
    expect(cmd).toContain('anio=2025');
    expect(cmd).toContain('dv=4');
  });

  // El SII responde ISO-8859-1 incluso en JSON. Leer como UTF-8 corrompe
  // cualquier texto con tilde, y el daño aparece lejos de la causa.
  it('decodifica la respuesta como ISO-8859-1', async () => {
    mockExec.mockReturnValue('' as never);
    const { client } = makeClient();

    await client.get('https://loa.sii.cl/cgi_IMT/X.cgi');

    const opts = mockExec.mock.calls[0][1] as { encoding?: string };
    expect(opts.encoding).toBe('latin1');
  });
});

describe('SiiHttpClient.postForm', () => {
  it('manda los campos como application/x-www-form-urlencoded', async () => {
    mockExec.mockReturnValue('<html>ok</html>' as never);
    const { client } = makeClient();

    await client.postForm('https://loa.sii.cl/cgi_IMT/Y.cgi', {
      rut_arrastre: '11111111',
      cbmesinformemensual: '03',
    });

    const cmd = String(mockExec.mock.calls[0][0]);
    expect(cmd).toContain('-d "rut_arrastre=11111111&cbmesinformemensual=03"');
  });

  it('escapa los valores con caracteres especiales', async () => {
    mockExec.mockReturnValue('<html>ok</html>' as never);
    const { client } = makeClient();

    await client.postForm('https://loa.sii.cl/cgi_IMT/Y.cgi', { glosa: 'a b&c' });

    const cmd = String(mockExec.mock.calls[0][0]);
    expect(cmd).toContain('glosa=a%20b%26c');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx jest tests/http.test.ts`
Expected: FAIL — no existe `src/http.ts`

- [ ] **Step 3: Write minimal implementation**

```typescript
// src/http.ts
import { execSync } from 'child_process';
import { SessionManager } from './session';

const TIMEOUT_MS = 30_000;

// El SII responde ISO-8859-1 en todo, incluidas las APIs que declaran JSON.
const ENCODING = 'latin1' as const;

// Transporte HTTP contra el SII. No sabe nada de ningún dominio del portal ni
// de cómo se autenticó la sesión: sólo pide el cookie jar a su dueño.
export class SiiHttpClient {
  constructor(private session: SessionManager) {}

  async get(url: string, params?: Record<string, string>): Promise<string> {
    const query = params ? `?${this.encodeParams(params)}` : '';
    return this.curl(`"${url}${query}"`);
  }

  async postForm(url: string, campos: Record<string, string>): Promise<string> {
    return this.curl(`-d "${this.encodeParams(campos)}" "${url}"`);
  }

  private async curl(args: string): Promise<string> {
    const jar = await this.session.rutaCookieJar();
    return execSync(
      `curl -sk -b "${jar}" -L --max-redirs 5 --max-time 25 ${args}`,
      { encoding: ENCODING, timeout: TIMEOUT_MS }
    ).toString();
  }

  private encodeParams(params: Record<string, string>): string {
    return Object.entries(params)
      .map(([k, v]) => `${encodeURIComponent(k)}=${encodeURIComponent(v)}`)
      .join('&');
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx jest tests/http.test.ts`
Expected: PASS (5 tests)

- [ ] **Step 5: Commit**

```bash
git add src/http.ts tests/http.test.ts
git commit -m "feat: agregar SiiHttpClient con sesión compartida y encoding ISO-8859-1"
```

---

### Task 3: Fixtures anonimizadas del informe anual

Antes del parser hacen falta los datos contra los cuales probarlo. Se separa en su propia tarea porque la anonimización tiene un criterio propio y un chequeo que la respalda.

**Files:**
- Create: `tests/fixtures/bhe-informe-anual.html`
- Create: `tests/fixtures/bhe-informe-anual-vacio.html`
- Test: `tests/fixtures/fixtures.test.ts`

**Interfaces:**
- Produces: dos fixtures con la estructura real del CGI y datos ficticios.

- [ ] **Step 1: Crear la fixture con datos**

RUT `11111111-1`, nombre ficticio, montos redondos. La estructura —nombres de variables, orden, comillas, el `document.write` alrededor— es idéntica a la respuesta real; sólo cambian los valores.

```html
<!-- tests/fixtures/bhe-informe-anual.html -->
<html><head><title>INFORME ANUAL DE BOLETAS DE HONORARIOS ELECTRONICAS</title></head>
<body>
<script>
 xml_values['nombre_contribuyente']      = "JUAN PEREZ SOTO ";
 xml_values['rut_arrastre']      	= "11111111";
 xml_values['dv_arrastre']       	= "1";
 xml_values['anio_consulta']		= "2025";
 xml_values['es_sociedad_profesionales']	= "NO";
 xml_values['ene1']= "1000000";
 xml_values['ene2']= "145000";
 xml_values['ene3']= "0";
 xml_values['ene4']= "101";
 xml_values['ene5']= "102";
 xml_values['ene6']= "2";
 xml_values['ene7']= "1";
 xml_values['feb1']= "2000000";
 xml_values['feb2']= "290000";
 xml_values['feb3']= "0";
 xml_values['feb4']= "103";
 xml_values['feb5']= "103";
 xml_values['feb6']= "1";
 xml_values['feb7']= "";
 xml_values['tot4']= "101";
 xml_values['tot5']= "103";
</script>
</body></html>
```

- [ ] **Step 2: Crear la fixture vacía**

Año sin boletas: la cabecera existe, las claves de meses no. Es una respuesta legítima, no un error.

```html
<!-- tests/fixtures/bhe-informe-anual-vacio.html -->
<html><head><title>INFORME ANUAL DE BOLETAS DE HONORARIOS ELECTRONICAS</title></head>
<body>
<script>
 xml_values['nombre_contribuyente']      = "JUAN PEREZ SOTO ";
 xml_values['rut_arrastre']      	= "11111111";
 xml_values['dv_arrastre']       	= "1";
 xml_values['anio_consulta']		= "2019";
 xml_values['es_sociedad_profesionales']	= "NO";
 xml_values['tot4']= "";
 xml_values['tot5']= "";
</script>
</body></html>
```

- [ ] **Step 3: Escribir el chequeo de anonimización**

```typescript
// tests/fixtures/fixtures.test.ts
import * as fs from 'fs';
import * as path from 'path';

// Las respuestas del SII traen RUT, nombres y montos reales. Este repositorio
// tiene licencia y PRs, así que una fixture cruda es una filtración. El
// chequeo corre sobre todas las fixtures para que ninguna se cuele después.
const RUT_DE_PRUEBA = /^1{8}$/;

describe('fixtures anonimizadas', () => {
  const dir = __dirname;
  const fixtures = fs.readdirSync(dir).filter(f => f.endsWith('.html'));

  it('hay fixtures que revisar', () => {
    expect(fixtures.length).toBeGreaterThan(0);
  });

  it.each(fixtures)('%s no contiene RUT fuera del rango de prueba', (nombre) => {
    const contenido = fs.readFileSync(path.join(dir, nombre), 'latin1');
    const ruts = [...contenido.matchAll(/\b(\d{7,8})\b\s*"?\s*;?\s*$/gm)]
      .map(m => m[1]);

    const sospechosos = ruts.filter(r => !RUT_DE_PRUEBA.test(r));
    expect(sospechosos).toEqual([]);
  });
});
```

- [ ] **Step 4: Correr el chequeo**

Run: `npx jest tests/fixtures/fixtures.test.ts`
Expected: PASS (2 tests). Si falla, la fixture tiene un RUT real: reemplazarlo por `11111111` respetando el largo.

- [ ] **Step 5: Commit**

```bash
git add tests/fixtures/
git commit -m "test: fixtures anonimizadas del informe anual de BHE"
```

---

### Task 4: Parser del informe anual

**Files:**
- Create: `src/scrapers/bhe.ts`
- Test: `tests/scrapers/bhe.test.ts`

**Interfaces:**
- Consumes: `SiiHttpClient.get()` (Tarea 2), `SessionManager.authenticateOnly()`.
- Produces:
  - `interface MesBhe { mes: number; honorarioBruto: number; retencionTerceros: number; retencionContribuyente: number; folioInicial: number | null; folioFinal: number | null; emisionesVigentes: number; emisionesAnuladas: number; }`
  - `interface InformeAnualBhe { anio: number; rut: string; nombreContribuyente: string; meses: MesBhe[]; folioInicial: number | null; folioFinal: number | null; }`
  - `class BheScraper { constructor(http: SiiHttpClient, session: SessionManager); informeAnual(anio: number): Promise<InformeAnualBhe> }`

- [ ] **Step 1: Write the failing test**

```typescript
// tests/scrapers/bhe.test.ts
import * as fs from 'fs';
import * as path from 'path';
import { BheScraper } from '../../src/scrapers/bhe';
import { SiiHttpClient } from '../../src/http';
import { SessionManager } from '../../src/session';

jest.mock('../../src/http');
jest.mock('../../src/session');

const MockHttp = SiiHttpClient as jest.MockedClass<typeof SiiHttpClient>;
const MockSession = SessionManager as jest.MockedClass<typeof SessionManager>;

function fixture(nombre: string): string {
  return fs.readFileSync(path.join(__dirname, '../fixtures', nombre), 'latin1');
}

function makeScraper(html: string) {
  const http = new MockHttp({} as any);
  const session = new MockSession({} as any, {} as any);
  // Se mockean ambos verbos: el informe anual usa GET y el mensual POST.
  (http.get as jest.Mock).mockResolvedValue(html);
  (http.postForm as jest.Mock).mockResolvedValue(html);
  (session.authenticateOnly as jest.Mock).mockResolvedValue(undefined);
  return { http, session, scraper: new BheScraper(http, session) };
}

describe('BheScraper.informeAnual', () => {
  it('parsea la cabecera del informe', async () => {
    const { scraper } = makeScraper(fixture('bhe-informe-anual.html'));

    const informe = await scraper.informeAnual(2025);

    expect(informe.anio).toBe(2025);
    expect(informe.rut).toBe('11111111-1');
    expect(informe.nombreContribuyente).toBe('JUAN PEREZ SOTO');
  });

  it('parsea los meses con actividad y omite los vacíos', async () => {
    const { scraper } = makeScraper(fixture('bhe-informe-anual.html'));

    const informe = await scraper.informeAnual(2025);

    expect(informe.meses).toHaveLength(2);
    expect(informe.meses[0]).toEqual({
      mes: 1,
      honorarioBruto: 1000000,
      retencionTerceros: 145000,
      retencionContribuyente: 0,
      folioInicial: 101,
      folioFinal: 102,
      emisionesVigentes: 2,
      emisionesAnuladas: 1,
    });
  });

  // Una columna ausente significa cero emisiones anuladas, no un dato perdido.
  it('trata las columnas vacías como cero', async () => {
    const { scraper } = makeScraper(fixture('bhe-informe-anual.html'));

    const informe = await scraper.informeAnual(2025);

    expect(informe.meses[1].emisionesAnuladas).toBe(0);
  });

  it('expone el rango de folios del año', async () => {
    const { scraper } = makeScraper(fixture('bhe-informe-anual.html'));

    const informe = await scraper.informeAnual(2025);

    expect(informe.folioInicial).toBe(101);
    expect(informe.folioFinal).toBe(103);
  });

  // Un año sin boletas es una respuesta legítima, no un fallo.
  it('devuelve lista vacía cuando el año no tiene boletas', async () => {
    const { scraper } = makeScraper(fixture('bhe-informe-anual-vacio.html'));

    const informe = await scraper.informeAnual(2019);

    expect(informe.meses).toEqual([]);
    expect(informe.folioInicial).toBeNull();
  });

  // Sin esto, una sesión caída o una página de error se parsean como "el año
  // no tuvo boletas", que es indistinguible de un año realmente vacío.
  it('falla si la respuesta no es un informe de BHE', async () => {
    const { scraper } = makeScraper('<html><body>Sesión expirada</body></html>');

    await expect(scraper.informeAnual(2025)).rejects.toThrow(/no devolvió un informe/);
  });

  it('consulta el CGI anual con el año pedido', async () => {
    const { scraper, http } = makeScraper(fixture('bhe-informe-anual.html'));

    await scraper.informeAnual(2025);

    const [url, params] = (http.get as jest.Mock).mock.calls[0];
    expect(url).toContain('TMBCOC_InformeAnualBhe.cgi');
    expect(params.cbanoinformeanual).toBe('2025');
  });

  it('autentica sin exigir selección de empresa', async () => {
    const { scraper, session } = makeScraper(fixture('bhe-informe-anual.html'));

    await scraper.informeAnual(2025);

    expect(session.authenticateOnly).toHaveBeenCalled();
    expect(session.getSession).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx jest tests/scrapers/bhe.test.ts`
Expected: FAIL — no existe `src/scrapers/bhe.ts`

- [ ] **Step 3: Write minimal implementation**

```typescript
// src/scrapers/bhe.ts
import { SiiHttpClient } from '../http';
import { SessionManager } from '../session';

export interface MesBhe {
  mes: number;
  honorarioBruto: number;
  retencionTerceros: number;
  retencionContribuyente: number;
  folioInicial: number | null;
  folioFinal: number | null;
  emisionesVigentes: number;
  emisionesAnuladas: number;
}

export interface InformeAnualBhe {
  anio: number;
  rut: string;
  nombreContribuyente: string;
  meses: MesBhe[];
  folioInicial: number | null;
  folioFinal: number | null;
}

const BASE = 'https://loa.sii.cl/cgi_IMT';
const CGI_ANUAL = `${BASE}/TMBCOC_InformeAnualBhe.cgi`;

const MESES = ['ene', 'feb', 'mar', 'abr', 'may', 'jun',
               'jul', 'ago', 'sep', 'oct', 'nov', 'dic'];

// El CGI devuelve la tabla vacía y escribe los datos en variables JavaScript
// que el navegador renderiza con document.write. El parser va sobre el fuente.
const XML_VALUE = /xml_values\['(\w+)'\]\s*=\s*"([^"]*)"/g;

export class BheScraper {
  constructor(
    private http: SiiHttpClient,
    private session: SessionManager
  ) {}

  async informeAnual(anio: number): Promise<InformeAnualBhe> {
    await this.session.authenticateOnly();
    const html = await this.http.get(CGI_ANUAL, {
      cbanoinformeanual: String(anio),
    });

    const values = this.parseXmlValues(html);

    // Una respuesta sin la cabecera del informe no es un año vacío: es una
    // sesión caída, un error del portal o un rediseño. Confundirlos haría que
    // el fallo se reporte como "no tenés boletas".
    if (!values['anio_consulta']) {
      throw new Error(
        'El SII no devolvió un informe de boletas de honorarios. La sesión pudo expirar; reintentá.'
      );
    }

    const meses = MESES
      .map((prefijo, i) => this.parseMes(values, prefijo, i + 1))
      .filter((m): m is MesBhe => m !== null);

    return {
      anio: this.toInt(values['anio_consulta']) ?? anio,
      rut: `${values['rut_arrastre'] ?? ''}-${values['dv_arrastre'] ?? ''}`,
      nombreContribuyente: (values['nombre_contribuyente'] ?? '').trim(),
      meses,
      folioInicial: this.toInt(values['tot4']),
      folioFinal: this.toInt(values['tot5']),
    };
  }

  private parseXmlValues(html: string): Record<string, string> {
    const values: Record<string, string> = {};
    for (const m of html.matchAll(XML_VALUE)) {
      values[m[1]] = m[2];
    }
    return values;
  }

  // Un mes sin folios no tuvo actividad: sus claves vienen ausentes o vacías.
  private parseMes(
    values: Record<string, string>,
    prefijo: string,
    mes: number
  ): MesBhe | null {
    const folioInicial = this.toInt(values[`${prefijo}4`]);
    if (folioInicial === null) return null;

    return {
      mes,
      honorarioBruto: this.toInt(values[`${prefijo}1`]) ?? 0,
      retencionTerceros: this.toInt(values[`${prefijo}2`]) ?? 0,
      retencionContribuyente: this.toInt(values[`${prefijo}3`]) ?? 0,
      folioInicial,
      folioFinal: this.toInt(values[`${prefijo}5`]),
      emisionesVigentes: this.toInt(values[`${prefijo}6`]) ?? 0,
      emisionesAnuladas: this.toInt(values[`${prefijo}7`]) ?? 0,
    };
  }

  private toInt(text: string | undefined): number | null {
    if (!text) return null;
    const digits = text.replace(/[^\d]/g, '');
    return digits ? parseInt(digits, 10) : null;
  }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx jest tests/scrapers/bhe.test.ts`
Expected: PASS (8 tests)

- [ ] **Step 5: Commit**

```bash
git add src/scrapers/bhe.ts tests/scrapers/bhe.test.ts
git commit -m "feat: parsear el informe anual de boletas de honorarios"
```

---

### Task 5: Tool `sii_bhe_resumen` y cableado

**Files:**
- Create: `src/tools/bhe.ts`
- Modify: `src/server.ts`
- Test: `tests/tools/bhe.test.ts`

**Interfaces:**
- Consumes: `BheScraper.informeAnual(anio: number)` (Tarea 4).
- Produces: `registerBheTools(server: McpServer, scraper: BheScraper): void`, que registra la tool `sii_bhe_resumen` con parámetro `anio: number`.

- [ ] **Step 1: Write the failing test**

```typescript
// tests/tools/bhe.test.ts
import { registerBheTools } from '../../src/tools/bhe';
import { BheScraper } from '../../src/scrapers/bhe';

jest.mock('../../src/scrapers/bhe');

const MockScraper = BheScraper as jest.MockedClass<typeof BheScraper>;

function makeServer() {
  const tools: Record<string, { descripcion: string; handler: Function }> = {};
  const server = {
    tool: (nombre: string, descripcion: string, _schema: unknown, handler: Function) => {
      tools[nombre] = { descripcion, handler };
    },
  };
  return { server, tools };
}

describe('registerBheTools', () => {
  it('registra sii_bhe_resumen', () => {
    const { server, tools } = makeServer();

    registerBheTools(server as any, new MockScraper({} as any, {} as any));

    expect(Object.keys(tools)).toContain('sii_bhe_resumen');
  });

  // Las tools de persona natural no dependen de la empresa seleccionada, y la
  // descripción es lo único que ve el modelo para decidir cuándo usarlas.
  it('describe la tool como colgando de la persona, no de la empresa', () => {
    const { server, tools } = makeServer();

    registerBheTools(server as any, new MockScraper({} as any, {} as any));

    expect(tools['sii_bhe_resumen'].descripcion).toMatch(/SII_EMPRESA_RUT/);
  });

  it('devuelve el informe del año pedido como JSON', async () => {
    const { server, tools } = makeServer();
    const scraper = new MockScraper({} as any, {} as any);
    (scraper.informeAnual as jest.Mock).mockResolvedValue({
      anio: 2025,
      rut: '11111111-1',
      nombreContribuyente: 'JUAN PEREZ SOTO',
      meses: [],
      folioInicial: null,
      folioFinal: null,
    });

    registerBheTools(server as any, scraper);
    const result = await tools['sii_bhe_resumen'].handler({ anio: 2025 });

    expect(scraper.informeAnual).toHaveBeenCalledWith(2025);
    expect(JSON.parse(result.content[0].text).anio).toBe(2025);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx jest tests/tools/bhe.test.ts`
Expected: FAIL — no existe `src/tools/bhe.ts`

- [ ] **Step 3: Write minimal implementation**

```typescript
// src/tools/bhe.ts
import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import { z } from 'zod';
import { BheScraper } from '../scrapers/bhe';

export function registerBheTools(server: McpServer, scraper: BheScraper): void {
  server.tool(
    'sii_bhe_resumen',
    'Resumen anual de las boletas de honorarios electrónicas emitidas por el RUT persona autenticado en el SII. Devuelve, por cada mes con actividad, el honorario bruto, la retención de terceros y del contribuyente, el rango de folios y cuántas boletas están vigentes o anuladas. No requiere SII_EMPRESA_RUT: cuelga de la persona, no de la empresa.',
    { anio: z.number().int().min(2000).max(2100).describe('Año tributario a consultar') },
    async ({ anio }: { anio: number }) => {
      const informe = await scraper.informeAnual(anio);
      return {
        content: [{ type: 'text' as const, text: JSON.stringify(informe, null, 2) }],
      };
    }
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx jest tests/tools/bhe.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 5: Cablear en el servidor**

En `src/server.ts`, agregar los imports y la construcción. `SiiHttpClient` recibe el mismo `session` que el resto, que es lo que garantiza una sola sesión por proceso.

```typescript
import { SiiHttpClient } from './http';
import { BheScraper } from './scrapers/bhe';
import { registerBheTools } from './tools/bhe';

// dentro de createServer(), después de crear `session`:
  const http = new SiiHttpClient(session);
  const bheScraper = new BheScraper(http, session);

// junto a los demás register*:
  registerBheTools(server, bheScraper);
```

- [ ] **Step 6: Verificar build y suite completa**

Run: `npx tsc --noEmit && npx jest`
Expected: todo verde.

- [ ] **Step 7: Commit**

```bash
git add src/tools/bhe.ts src/server.ts tests/tools/bhe.test.ts
git commit -m "feat: registrar la tool sii_bhe_resumen"
```

---

### Task 6: Detalle mensual — `sii_bhe_list_emitidas` y `sii_bhe_list_recibidas`

El informe anual resume por mes; el detalle boleta por boleta sale del informe mensual. El CGI y sus parámetros están verificados, pero **la estructura de la respuesta mensual no se capturó en la spike**: sólo se confirmó el CGI destino. Por eso esta tarea empieza capturando la fixture real.

**Files:**
- Modify: `src/scrapers/bhe.ts`
- Modify: `src/tools/bhe.ts`
- Create: `tests/fixtures/bhe-informe-mensual.html`
- Test: `tests/scrapers/bhe.test.ts` (agregar describe)

**Interfaces:**
- Consumes: todo lo de las Tareas 2 y 4.
- Produces:
  - `interface BoletaBhe { folio: number; fecha: string; receptorRut: string; receptorNombre: string; honorarioBruto: number; retencion: number; totalLiquido: number; anulada: boolean; }`
  - `BheScraper.informeMensual(anio: number, mes: number, recibidas?: boolean): Promise<BoletaBhe[]>`

- [ ] **Step 1: Capturar la respuesta mensual real**

Con la sesión autenticada, pedir el CGI mensual y guardar la respuesta cruda **fuera del repositorio**:

```bash
curl -sk -b /tmp/sii_cookies.txt \
  -d "rut_arrastre=<RUT>&dv_arrastre=<DV>&cbmesinformemensual=01&cbanoinformemensual=2025" \
  https://loa.sii.cl/cgi_IMT/TMBCOC_InformeMensualBhe.cgi > /tmp/mensual-crudo.html
```

- [ ] **Step 2: Identificar dónde vienen los datos**

Igual que el anual, es probable que estén en `xml_values`, pero con una clave por boleta en vez de por mes. Inspeccionar con:

```bash
grep -a -o "xml_values\['[^']*'\]" /tmp/mensual-crudo.html | sort -u | head -40
```

`grep -a` no es opcional: el archivo es ISO-8859-1 y sin ese flag `grep` lo trata como binario y no devuelve nada.

- [ ] **Step 3: Anonimizar y versionar la fixture**

Reemplazar RUT por `11111111`, nombres por ficticios y montos por valores redondos, **conservando el largo y el formato de cada campo**. Guardar en `tests/fixtures/bhe-informe-mensual.html` y correr el chequeo:

Run: `npx jest tests/fixtures/fixtures.test.ts`
Expected: PASS — si falla, quedó un RUT real.

- [ ] **Step 4: Escribir los tests del parser mensual**

Con la estructura ya conocida del paso 2, escribir tests análogos a los del anual: parseo con datos, mes vacío, respuesta que no es un informe, y que `recibidas: true` apunte al CGI con sufijo `Rec`. Como mínimo:

```typescript
describe('BheScraper.informeMensual', () => {
  it('usa el CGI de emitidas por defecto', async () => {
    const { scraper, http } = makeScraper(fixture('bhe-informe-mensual.html'));
    await scraper.informeMensual(2025, 1);
    expect((http.postForm as jest.Mock).mock.calls[0][0])
      .toContain('TMBCOC_InformeMensualBhe.cgi');
  });

  it('usa el CGI con sufijo Rec para las recibidas', async () => {
    const { scraper, http } = makeScraper(fixture('bhe-informe-mensual.html'));
    await scraper.informeMensual(2025, 1, true);
    expect((http.postForm as jest.Mock).mock.calls[0][0])
      .toContain('TMBCOC_InformeMensualBheRec.cgi');
  });

  it('rellena el mes a dos dígitos, como espera el formulario', async () => {
    const { scraper, http } = makeScraper(fixture('bhe-informe-mensual.html'));
    await scraper.informeMensual(2025, 3);
    expect((http.postForm as jest.Mock).mock.calls[0][1].cbmesinformemensual)
      .toBe('03');
  });
});
```

- [ ] **Step 5: Implementar el lado de la petición**

Este lado está verificado, así que va completo. Sólo el cuerpo de `parseBoletas` depende de lo que se descubra en el paso 2.

```typescript
// En src/scrapers/bhe.ts

export interface BoletaBhe {
  folio: number;
  fecha: string;
  receptorRut: string;
  receptorNombre: string;
  honorarioBruto: number;
  retencion: number;
  totalLiquido: number;
  anulada: boolean;
}

const CGI_MENSUAL = `${BASE}/TMBCOC_InformeMensualBhe.cgi`;
const CGI_MENSUAL_REC = `${BASE}/TMBCOC_InformeMensualBheRec.cgi`;

  async informeMensual(
    anio: number,
    mes: number,
    recibidas = false
  ): Promise<BoletaBhe[]> {
    await this.session.authenticateOnly();
    const html = await this.http.postForm(
      recibidas ? CGI_MENSUAL_REC : CGI_MENSUAL,
      {
        // El formulario del portal manda el mes con dos dígitos; mandarlo con
        // uno devuelve el informe vacío en vez de un error, que es la peor
        // combinación posible.
        cbmesinformemensual: String(mes).padStart(2, '0'),
        cbanoinformemensual: String(anio),
      }
    );

    const values = this.parseXmlValues(html);
    if (!values['anio_consulta']) {
      throw new Error(
        'El SII no devolvió un informe de boletas de honorarios. La sesión pudo expirar; reintentá.'
      );
    }

    return this.parseBoletas(values);
  }
```

`rut_arrastre` y `dv_arrastre` no se pasan: el SII los inyecta server-side desde la sesión, como se verificó en la spike.

- [ ] **Step 6: Implementar `parseBoletas` según la estructura descubierta**

Escribir el cuerpo de `parseBoletas(values: Record<string, string>): BoletaBhe[]` a partir de las claves reales que arrojó el paso 2, y los tests que las cubran. Mantener el criterio del informe anual: una clave ausente es cero o ausencia legítima, nunca un error.

- [ ] **Step 7: Registrar las tools**

```typescript
// En src/tools/bhe.ts, dentro de registerBheTools

  const paramsMes = {
    anio: z.number().int().min(2000).max(2100).describe('Año a consultar'),
    mes: z.number().int().min(1).max(12).describe('Mes a consultar (1-12)'),
  };

  server.tool(
    'sii_bhe_list_emitidas',
    'Lista boleta por boleta las boletas de honorarios electrónicas emitidas por el RUT persona autenticado en un mes: folio, fecha, receptor, honorario bruto, retención, total líquido y si está anulada. No requiere SII_EMPRESA_RUT.',
    paramsMes,
    async ({ anio, mes }: { anio: number; mes: number }) => ({
      content: [{
        type: 'text' as const,
        text: JSON.stringify(await scraper.informeMensual(anio, mes), null, 2),
      }],
    })
  );

  server.tool(
    'sii_bhe_list_recibidas',
    'Lista las boletas de honorarios electrónicas recibidas por el RUT persona autenticado en un mes, con el mismo detalle que las emitidas. No requiere SII_EMPRESA_RUT.',
    paramsMes,
    async ({ anio, mes }: { anio: number; mes: number }) => ({
      content: [{
        type: 'text' as const,
        text: JSON.stringify(await scraper.informeMensual(anio, mes, true), null, 2),
      }],
    })
  );
```

- [ ] **Step 8: Verificar**

Run: `npx tsc --noEmit && npx jest`
Expected: todo verde.

- [ ] **Step 9: Commit**

```bash
git add src/scrapers/bhe.ts src/tools/bhe.ts tests/
git commit -m "feat: agregar el detalle mensual de boletas emitidas y recibidas"
```

---

### Task 7: Spike de emisión — resolver los contratos, sin implementar

La emisión es el único acto de escritura del plan y no tiene contrato verificado. Esta tarea **no produce tools**: produce los datos con los que se planifica la emisión.

**Files:**
- Create: `docs/superpowers/specs/2026-08-01-bhe-emision-contratos.md`

- [ ] **Step 1: Mapear el formulario del paso 2**

Con sesión activa, pedir `TMBECN_PresentaDatosBoleta.cgi` (POST con `rut_arrastre`, `dv_arrastre`, `OptTipoRetencion=RETRECEPTOR`, `TipoEmision=1`) y listar todos los campos del formulario, incluidos los ocultos.

Es una petición de sólo lectura: devuelve el formulario vacío y no involucra receptor.

- [ ] **Step 2: Documentar la cadena completa**

Registrar, para cada uno de los cinco pasos, su CGI, método y campos:

1. `TMBECN_ValidaTimbrajeContrib.cgi` — elegir tipo de retención
2. `TMBECN_PresentaDatosBoleta.cgi` — formulario de datos
3. `TMBECN_ConfirmaTimbrajeContrib.cgi` — previsualiza, **no emite**
4. `TMBECN_BoletaHonorariosElectronica.cgi` — **emite**
5. `TMBECN_PresentaDatosEnvio.cgi` / `TMBECN_EnviarBoleta.cgi` — envío al receptor

- [ ] **Step 3: Determinar qué hace `tiempo`**

El paso 2 inyecta un `tiempo` con un timestamp Unix. Verificar si es anti-replay comparando dos cargas sucesivas del formulario. Mientras no se sepa, la regla es propagarlo tal cual y no regenerarlo.

- [ ] **Step 4: NO avanzar al paso 4**

El paso 3 previsualiza y es seguro. El paso 4 emite un documento tributario real e irreversible que además notifica al receptor. **No se ejecuta durante la spike.** Tampoco se emite "para probar" con intención de anular: una BHE emitida y anulada deja rastro en el SII y notifica igual.

- [ ] **Step 5: Commit del documento**

```bash
git add docs/superpowers/specs/2026-08-01-bhe-emision-contratos.md
git commit -m "docs: contratos del flujo de emisión de BHE"
```

---

## Verificación final de la fase

- [ ] `npx tsc --noEmit` sin errores
- [ ] `npx jest` con toda la suite verde
- [ ] `npm run build` genera `dist/`
- [ ] Prueba manual contra el SII real: `sii_bhe_resumen` con un año con boletas y otro sin ellas; el segundo devuelve lista vacía, no error
- [ ] `sii_cerrar_sesion` al terminar la prueba manual, para no dejar sesiones abiertas
- [ ] Ninguna fixture contiene RUT, nombres ni montos reales
